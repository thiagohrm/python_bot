version: '3.9'
services:
  app:
    build: .
    container_name: python_bot_app
    volumes:
      - .:/app
    command: python main.py
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
